# Acceptance log

Every dated trial for the peer Swarm-SLAM, native MOLA and MGG planning work, in
reverse chronological order. Numbers are reproduced as written in the source
records; where a number could not be carried over it was dropped rather than
paraphrased. The architecture, invariants and open gates are in
[the plan](../plan.md). The original chronological records are kept unchanged in
[the archive](../archive/README.md).

Dates come from the source record when it states one, otherwise from the commit
date of the named revision. Some records are not strictly chronological within a
day; rows are then ordered by the source's own narrative sequence.

The 2026-09-21 qualification used `sroy@benchbot`, the isolated Compose
project `swarmdeck-priority20260921`, DDS domain 173 and loopback API/UI ports
28080/25173. Existing `planning-next` and `swarmdeck` deployments were not
replaced. Evidence is retained under
`/home/sroy/workspaces/swarmdeck-priority20260921/.qualification/` on Benchbot;
the final traces and derived truth checks are in `runtime-backbone/sessions/`.

| Date | Revision / images | Scenario and mission | Result | What it does not establish |
| --- | --- | --- | --- | --- |
| 2026-09-21 (reset during motion) | Final epoch/reset implementation; MGG `motion-backbone20260921` | Bistro mission `ffd44248-605b-497b-bdd4-7c36b2c52dad`; R0 actively executing Explore before reset request `3552fdbf-49d1-4a7d-881a-c72d92cd80fa` | Target epoch 0→1; ready/idle in 24.06 s. Other three epochs/runs and captured keyframes retained; stale replica replay rejected with 409; same UUID replay returned done. Independent truth: maximum 0.0165 m XY displacement from quiescence through 10 s after done, 155 samples. `benchbot3-active-reset.json` and `benchbot3-final-truth-analysis.json`. | Hardware reset; an instantaneous physical stop at request receipt. |
| 2026-09-21 (final long returns) | MGG `7004cd4` plus `mgg-motion-reliability.patch`, runtime `motion-backbone20260921` | Same final mission, after the 180 s Explore trial; Home order R1/R3/R0/R2, 420 s bound per robot | R1/R3/R0 succeeded in 71.9/112.3/111.1 s; independent return errors 0.240/0.368/0.283 m against the original pre-motion recording. All exercised rolling sections. Scout refused in 1 s: current pose `(-21.65, -21.26, 0.25)` outside the 1 m graph tolerance, no motion, 29.916 m physical distance remaining. Native logs show its breadcrumb drain blocked and later exploration paths could not link to the graph. `benchbot3-long-home.json`; independent alignment in `benchbot3-final-truth-analysis.json`. | The fleet-wide Home gate is **not** closed. No safety limit was raised to bypass the Scout refusal. |
| 2026-09-21 (final Explore) | Same final native source; peer closures off | Final four-robot Bistro mission, 180 s Fleet Explore after the initial Home trials | All four passed: navigation-frame maximum displacement R0/R1/R2/R3 30.157/23.660/29.566/30.876 m. Independent truth over the complete 181.38 s command window, including startup and Stop All: 30.180/23.622/30.093/31.261 m. No authority change or authority transient; one HTTP 404 observation error. Stop All verified. `benchbot3-explore.json` and `fleet-truth-benchbot3-explore.json`. | Uninterrupted motion: R1 also waited for automatic no-path replanning. Full coverage, merged-frame accuracy, or hardware. |
| 2026-09-21 (final Navigate and initial Home) | Final checked-in native motion correction; runtime `motion-backbone20260921` | Final four-robot Bistro mission; 3 m, then 8 m forward probes, then Home | 3 m: R0/R2/R3 succeeded; R1 refused an over-bound indexed provisional connector. 8 m: R0/R1 succeeded (7.804/7.782 m maximum displacement); Scout moved 8.766 m before recovery exhausted at a measured 0.581 m rise, and Spot refused a 1.266–1.573 m drop without moving. Subsequent Home physically returned all four: R0/R1/R2/R3 errors 0.251/0.245/0.241/0.225 m. The live-envelope harness passed only three: Scout was inconclusive at 0.621 m despite controller success and its independent physical arrival. Stop All verified for every probe. `benchbot3-{navigate3,navigate8,home,truth-analysis}.json`. | Four passing Home harness results; universal cold-start or terrain admission. A preceding `motion-verified20260921` cycle passed all four 3 m probes and three 8 m probes but failed all four Home graph bindings; the final backbone correction addresses that failure without declaring long returns universally solved. |
| 2026-09-21 (fleet-safe map epochs) | Durable per-robot epochs, native cslam retirement, reset supervisor and UI | Merged mission `38afd1c1-6da6-40c9-964b-0cb46d14296b`, plus unmerged UI/reset mission `622da7b1-ecab-410f-9ca4-8265348a880e` | Real native four-peer merge verified before R0 reset: epoch 0→1 in 21.21 s; old target anchors retired from all three peer replicas while their own keyframes/runs remained. Direct restart of that peer advanced 1→2 and restored ready authority in 16.36 s. Stale replicas returned 409. Manual R2 reset took 21.75 s, followed by successful Navigate and Home to the new reset location. Actual UI R1 reset took 20.429 s, disabled the pending action and restored it on completion. Native descriptor/MAC-index/closure retirement and durable stale-result fencing smoke passed. | Inter-robot metric accuracy, multi-host partition recovery or a qualified hardware supervisor. Automatic restart was not an API idempotency test. |
| 2026-09-21 (signed builds and regression) | Jazzy 2026-06-18 ROS snapshot; MGG remains pinned at `7004cd47c9197a44ab89d4ee1d7023418c957141` with the checked-in motion patch | Benchbot amd64, fresh sim/mapping/MGG/cslam dependency layers; final production MGG manifest `eda62c37afc0ce92aaf401957d6ad2878cbb457238de50c950f2e3e5972d389f` | Four Jazzy runtime images built without the obsolete cache anchors. Final production MGG applied the complete patch to the pinned clone and built six packages; its actual PCI/DDS start/stop, terminal-state and late-response smoke passed. Final native core 16/16 and ROS 3/3 CTest targets passed. Native MOLA tests and real indexed-query smoke passed; lifecycle lost-reply/inactive recovery and SIGINT smoke passed. Python regression: 1860 passed, 2 skipped. UI: 92 tests, zero Svelte errors/warnings, production build passed. | A hermetic OS/Python lock, ARM or physical-platform qualification. Native baseline compiler warnings and the UI chunk-size warning remain. |
| 2026-09-20 (empty section refused, kerb body volume) | `2fe855e`; MGG `7004cd4` (a section whose proxy bottoms out on measured terrain is refused; the index server's body volume starts above the platform's climbable band) | Benchbot Bistro, one cycle with closures on, image `mola-cleanup-2fe855e` | 3 m 4 of 4; 8 m 2 of 4 (robot_0 7.8 m, the Spot 8.0 m, its first; robot_1's blind goal on a 0.27 m rise; the Scout 6.9 of 8 m then no grid detour). Explore 52.4 / 26.6 / 38.4 / 0.0 m. Returns 3 of 4: robot_0 56.0 m home in 158 s, robot_1 26.6 m in 164 s, the Scout 43.9 m in 154 s; the Spot, parked by its 8 m sweep beside a 0.8 m rise, never explored (`current pose cannot be linked to the global graph` 17 times, `body includes unknown space` 3) and its return was refused on segment 0->1 for that rise. No replan loop, no empty-section refusal needed, no own-cell occupancy; 53 of 56 route rejections were index re-index windows. | The best long test so far; one sample. The Spot's parking beside a wall is a start-connector case (its body volume includes the wall's unknown shadow). |
| 2026-09-20 (micron slack) | `54dee46`; MGG `8045f61` | Benchbot Bistro, one cycle with closures on, image `mola-cleanup-54dee46` | 3 m 4 of 4; 8 m 0 of 4: robot_1 and the Scout cut by the adapter's replan bound after 4 s with the dispatched path named, `2 poses from (2.78, 0.03) to (2.78, 0.01)`, and MGG's own line `objective section: 2 poses ... 0.02 m from the robot (partial)`: the section proxy had stepped back onto the robot; robot_0 6.7 m then a route waypoint whose body intersects known occupied space; the Spot's blind goal on a rise. Explore 0.3 / 19.2 / 60.1 / 14.7 m. Returns 0 of 4: robot_0 and robot_1 refused at their own cells (`occupied at sample 0`, beside kerbs), the Scout 51.5 of 67 m then no traversable grid detour, the Spot refused for a known rise on segment 0->1. | Two causes identified and fixed for the next build: MGG `7004cd4` (the empty section is refused) and `d4ddfb3` (the index server's body volume starts above the climbable band). |
| 2026-09-20 (section logging) | `46b6bf8`; MGG `5934234` (each objective section's ends logged) | Benchbot Bistro, one cycle with closures on, image `mola-cleanup-46b6bf8` | 3 m 4 of 4; 8 m 3 of 4 (robot_0 7.8, robot_1 7.8, the Scout 7.8 m; the Spot's blind goal on a 0.58 m rise), no replan loop. Explore 5.8 / 41.9 / 37.3 / 38.0 m. Returns 1 of 4: the Scout 42.5 m home in 173 s; robot_1 43.7 of 49 m then a route waypoint without mapped ground support 5.7 m from home; robot_0 refused at its own cell (occupied at (8.42, -2.03)); the Spot 7.7 m then `emitted body height mismatch 0.100000 m exceeds 0.100000 m`, the float32 rounding of a mismatch exactly at the tolerance, which also refused its 3 m goal on the previous cycle (fixed by the next MGG pin, a micron of slack). 46 route shortcuts. | Returns now fail late (44 of 49 m) or at the start, not in the middle; the terrace table trap and the own-cell occupancy remain to be understood. |
| 2026-09-20 (straightened returns) | `15dbed1`; MGG `0406633` (explicit and global-frontier routes shortcut across space the map has seen traversable, as exploration paths were; the level-gated attitude prior; the merged 2D raster sweeping keyframe rays free with a per-scope `seq`) | Benchbot Bistro, one cycle with closures on, image `mola-cleanup-15dbed1` | 3 m 3 of 4 (the Spot refused at once: emitted body height mismatch 0.100005 m over a 0.100000 m limit); 8 m robot_0 7.8 m, robot_1 cut by the adapter's replan bound after 4 s (`7.98 m from the section endpoint, odometry 0.0 s old`: three FollowPath goals each reached by Nav2 3 ms after receipt, right after a completed 3 m goal, the same signature as robot_2 on `4a0b73a` and robot_1 on `6f74de1`), the Scout and the Spot refused at their blind goals. Explore 29.2 / 37.5 / 16.6 / 35.5 m. Returns 2 of 4 over long, straight routes: robot_0 34.3 m home in 110 s, the Spot 34.5 m in 152 s, robot_1 36.3 of 39 m before an objective request timed out; the Scout refused at its own cell, occupied, under a terrace table it had explored beneath. 17 route shortcuts (`49 poses -> 4`, `61 -> 4`). The merged raster covered 1844 m2 against 1418 m2 on the previous build (the largest local map 2099 m2) and read as an occupancy map. | The Nav2 instant completion is bounded and diagnosed on both sides now (`2c3366d` logs each section's ends), not explained. One cycle is one sample. |
| 2026-09-20 (roadmap restored) | `7803305`; MGG `5e9f2d0` (global graph expansion timer and odometry mesh of the original rrg.cpp restored; roadmap edges stop at unknown; reference paths projected to the common driving height; the occupied-or-unknown refusal names its sample) with the cslam upright prior | Benchbot Bistro, one cycle with inter-robot closures on, image `mola-cleanup-7803305` | 3 m 4 of 4; 8 m 1 of 4 (robot_0 7.8 m; robot_1 and the Spot refused for blind goals on a 0.34 m and a 0.62 m rise; the Scout 7.0 m then `mapping authority snapshot is missing, invalid, or stale`). Explore 45.6 / 34.2 / 27.6 / 31.2 m with all four moving. Returns 2 of 4: robot_1 35.7 m home in 124 s and the Scout 32.2 m in 163 s; robot_0 12.9 m of 50 then Nav2 made no progress for 30 s 0.32 m off the route; the Spot 5.9 m of 32 then no traversable grid detour. Global graphs of 161 to 401 vertices with edges well above vertices (robot_2: 401 vertices, 807 edges; expansion lines `global graph expansion: +1 vertices, +1 edges`). `indexed map route is occupied or unknown` fell from 35 to 1 of the route rejections; 43 of 52 rejections were the index server's re-index windows, which the adapter retries; no route-window replan loop. | The two remaining return failures are a controller stall and a local grid refusal, not roadmap or frame problems; one cycle is one sample. |
| 2026-09-20 (upright prior) | `8276f29`; MGG `7c6b11e` (cslam patch `cslam-upright-prior`: a gtsam::Pose3AttitudeFactor on every pose of the aggregate graph, sigma 0.01 rad) | Benchbot Bistro, one cycle with inter-robot closures on (cap 8 per pair, 58 closures per peer), image `mola-cleanup-8276f29` | Every robot's `T_component_navigation` tilt fell from 0.86, 0.67, 1.46 and 0.11 degrees (measured on the previous cycle with `tiltprobe.py`) to 0.002, 0.003, 0.022 and 0.000 degrees with all six pairs merged. 3 m 4 of 4; 8 m 1 of 4 (robot_0 7.77 m, refused for tilt two cycles earlier; robot_1 and the Spot refused at once because their blind goals sit on a 0.32 m and a 0.58 m rise; the Scout 5.3 m then `occupied or unknown`). Explore 0.1 / 16.0 / 18.4 / 52.8 m; returns 0 of 4 (robot_0 and robot_1 refused at once with `indexed map route is occupied or unknown`, the Scout 1.3 m then the same, the Spot 9.4 m then no progress for 30 s). 35 of 63 route rejections were `indexed map route is occupied or unknown`. A probe of robot_1's home route (`valprobe.py`, 89 samples over 16 m) found every sample FREE with ground and 0.37 to 0.55 m of clearance, at the route height and 0.12 m higher, while the route's own vertices stood 0.12 to 0.30 m above the flat ground (two height conventions in the global graph), so consecutive poses differ by up to 0.18 m, above the 0.15 m step limit. | Tilt is no longer a cause. The remaining refusals are the planner's own: the vertex height mix and an `occupied` verdict on refined sections whose position the reason does not yet name. |
| 2026-09-20 (stale odometry guard) | `557f262`; MGG `7c6b11e` (plans and continuations refused once odometry is 5 s old; the route-window refusal names the distance and the odometry age; adapter bound of three window replans without motion; MGG `0da45b3` tilt tolerance 0.05 rad) | Benchbot Bistro, one cycle with closures on, image `mola-cleanup-557f262` | 3 m 4 of 4; 8 m 1 of 4 (the Spot 7.97 m; robot_0 and robot_1 refused in 4 s for no ground support at the goal; the Scout 3.75 m then recovery exhausted). Explore 14.5 / 43.3 / 49.4 / 6.1 m; returns 0 of 4. No replan loop occurred (zero window refusals), so the staleness diagnostics carried no data. The Spot's local grid went from 417 vertices to every cell rejected as `collision` within 2 s at sim time 1789903426 and stayed so for the rest of the cycle, while its latest product (r55) showed one clean floor and its authority tilt was 0.11 degrees; unexplained. Measured tilts on this cycle: 0.86, 0.67, 1.46, 0.11 degrees. | The tilt tolerance was the wrong lever: a tilt of 1.46 degrees accepted by the loader puts the floor 0.5 m off at 20 m. |
| 2026-09-20 (global graph restored) | `6f74de1`; MGG `7558a98` (every accepted exploration path added to the global graph, local frontier paths clustered and added, routing over the global graph to the best-gain global frontier when no local leaf has gain, a 0.02 m terrain measurement margin) | Benchbot Bistro, one cycle with inter-robot closures on (cap 8 per pair), image `mola-cleanup-6f74de1` | 3 m 4 of 4. 8 m 0 of 4: robot_0 had every plan refused with `footprint cell grid unavailable` because its merged authority frame was tilted 1.174 degrees and the MOLA loader tolerated 0.02 rad (1.15 degrees); robot_1 fell into the empty-section loop again (sections completed in milliseconds and replanned 2.5 times a second, intermittent: a later replay of the same goal produced a proper 8 m section); the Scout drove 2.8 m then `no ground support`; the Spot's goal lay on the building edge. Explore: robot_0 0 m (the tilt refusal), robot_1 8.3 m then blocked, the Scout 19.5 m, the Spot 44.4 m with its global graph growing to 320 vertices (`global graph:` lines) and the global planner routing to global frontiers when the local graph had no gain. Returns 1 of 4 (robot_1). | Whether the global roadmap improves coverage over a full session: one robot was blind by tilt and one was looping, so only two robots exercised the roadmap. The tilt tolerance (MGG `0da45b3`, 0.05 rad) and an adapter bound on route-window replans without motion (three in a row end the objective) are the next cycle. |
| 2026-09-20 (closures on, capped) | `f0981e7` then `faccb21`; MGG `b7927ea` (per-pair closure cap of 8 in the cslam peer; the empty-section refusal restored; the index view's point budget 2,000,000) | Benchbot Bistro, two cycles with inter-robot closures on, images `mola-cleanup-f0981e7` and `mola-cleanup-faccb21` | The cap holds: all six pairs reached 8 closures within two minutes and stopped (58 per peer, against 707 in twenty minutes before), cslam's peers fell from about 260% to 25% CPU each and the host load from 25 to 7; solver adoptions fell to 2 to 9 per robot against 66 reports. First cycle (`f0981e7`): 3 m 3 of 4, 8 m 1 of 4 with the two Bunkers standing in the empty-section loop for 120 s each (a validated prefix of no progress emitted as a section, completed by Nav2 in 4 ms, replanned 2.5 times a second: the refusal from `8929eb8` had gone with the rollback), Explore 20, 3, 53, 42 m, returns 1 of 4 (the Spot 34 m in 70 s; the Scout 48.5 m of 59 then `index refresh failed: index point budget exceeded`, the index view's own 1,000,000 cap). Second cycle (`faccb21`): 3 m 4 of 4, 8 m 1 of 4 (the Scout; the Bunkers refused on footprint terrain, the Spot's blind goal on the building edge), Explore 31, 32, 18, 33 m with all four moving, returns 1 of 4 (the Scout 11 m; robot_0 19 of 35 m then `occupied or unknown`, robot_1 17 of 36 m then Nav2 `Failed to make progress`, the Spot 11 of 34 m then `no observed traversable grid detour`). 38 of 52 route rejections were the index server's re-index windows after per-keyframe products (`a different snapshot failed indexed publication` 28, `requested snapshot is not current` 10), which the adapter retries. | Merges no longer break navigation by load or churn; what remains is the planner's own terrain refusals on this street, the same as without merges. The global graph work (path and frontier ingestion, global frontier replanning, a terrain measurement margin) is in progress in MGG. |
| 2026-09-19 (night, closures on) | `01828cb` to `ef39775` (inter-robot closures on for benchbot; merged component rasterized for the 2D map; merged view assembled across lagging publishers; live overlay fence tolerant of the product-gated authority's lag; 2D global view draws the displayed raster's robots; planner grid point budget 2,000,000 with `product_error` reporting; solver results adopted at 5 cm / 0.5 degrees, at most every 10 s unless large, the first always) | Benchbot Bistro, missions `1a8cc114` (first merged trial), `bd4362c4` (invalid: the anchor robot never adopted, merged view waiting), `ef39775` at `f6c7...`: one cycle of sweeps and the long test | With closures on the four robots merge within 40 s and both interfaces show one map with all four robots. Sweeps: 3 m 2 of 4 (the Scout refused inside its blind ring, the Spot's goal on the parked Scout), 8 m 0 of 4 (robot_0 5.6 m then `occupied or unknown`, robot_1 `objective request timed out` under host load 25 on 16 cores, the Scout 6.2 m then `body includes unknown space`, the Spot `no mapped ground support`). Explore 19, 19, 12 and 18 m. Returns one of four (the Spot, 18.7 m in 240 s). MGG rejected 58 routes in 12 minutes: 39 were snapshot or authority churn (`a different snapshot failed indexed publication` 23, `requested snapshot is not current` 13, `snapshot changed during planning` 3) despite adoptions falling to 24 to 27 per robot against 76 solver reports; 707 accepted inter-robot closures per peer in 20 minutes with cslam's registration nodes at 2.6 cores per peer. The 2D merged raster drew floor as occupied until the obstacle band's floor was raised to 0.30 m (`2ae575a`). | Navigation under merges is worse than without them; the churn and load point at cslam re-registering linked pairs (a per-pair cap is in progress). The Spot's own floor scatter and the Bistro kerbs remain. |
| 2026-09-19 (night, rollback) | `82cefe4` then `26a8d65`; MGG `ccda9ce` (the `0e189ed` tree: the raster stage, the drop limit and footprint asymmetry, breadcrumb skipping, empty-section refusal and the graph connectors reverted; the index server's terrain-band and blind-ring rules and the drop launch override reverted; the fleet composite keeps a member whose replica runs ahead of its authority; the 3D live map holds 256 MB and 6 M points) | Benchbot Bistro, mission `67b7aa87`, images `mola-cleanup-26a8d65`, one cycle of the same sweeps and long test | The operator's verdict on the day's planner work was that the added stages and special cases made the system a mess and the robots drove into obstacles, so it was rolled back. On the rolled-back planner: 3 m goals four of four, 8 m goals three of four (the Scout's goal on the kerb at the bend stayed active for 120 s without motion), Explore 19, 29, 33 and 42 m, returns none of four (robot_0 and robot_1 refused at once with `route corridor waypoint[1] rejected: no mapped ground support`, the Scout 12.9 m of 33 then `indexed map route is occupied or unknown`, the Spot 7 m then the waypoint rejection), which is the 2026-09-18 night state. The fleet composite listed all four robots (423 submaps) for the first time. | One cycle; the return-leg failure is the known state before yesterday's changes, now to be addressed within MGG's own concept rather than with added stages. |
| 2026-09-19 (evening) | `328bb24`; MGG `4181c58` (relief between the climb and drop limits is a costed step cell in the raster) | Benchbot Bistro, mission `b73cd52f`: the same cycle, images `mola-cleanup-328bb24` | Returns four of four for the first time: robot_0 51 m in 315 s, robot_1 48 m in 159 s, the Scout 41 m in 120 s, the Spot 36 m in 116 s, all four back within 0.6 m of their surveyed starts (ground truth). Explore 47, 44, 33 and 35 m. 3 m goals three of four (the Scout refused at once as `occupied` one metre ahead: inside its blind ring a lone ground return under the body counted as a collision without a terrain fit; fixed after the cycle in `6c1a1cc`, not in these images). 8 m goals two of four: the Scout's goal was inside a parked neighbour's disc and the Spot's blind straight-ahead goal lay on a building edge 1.6 m up; robot_0 and robot_1 completed theirs in all three cycles. The raster stage planned 167 routes this cycle and refused 6 (five `goal inside a transient disc`, one `goal obstacle`), every other objective ran through it. | Three cycles, one per image; the return rate went 1, 2, 4 of 4 as each cycle's measured cause was removed, so a further cycle on `6c1a1cc` is needed before calling it stable. The 8 m sweep's second row is not a planner measurement on this street. |
| 2026-09-19 (later) | `a6307fd`; MGG `2bb56ba` (index server: relief within the terrain band is not a collision, 1.0 s query budget; adapter retries a budget refusal; raster: inflated cells crossed at a cost, a missing terrain fit is a horizon) | Benchbot Bistro, mission `f97c8078`: the same cycle, images `mola-cleanup-a6307fd` | 3 m goals four of four (the Scout took 46 s), 8 m goals three of four (the Spot's goal lay 0.9 m from where robot_0 had parked: `goal inside a transient disc` was correct, ground truth confirmed the four goals crowd into one patch because the robots drift laterally on the bend), Explore 35, 37, 27 and 6.8 m (the Spot started boxed in), returns two of four: robot_1 41 m in 116 s and the Scout 35 m in 114 s, both along the kerb line that refused robot_1 in the morning; robot_0 drove 22.6 m of 37 then stalled with `known rise 0.241 / 0.468 m` from the footprint check on three near-identical raster routes; the Spot refused at once with `known rise 1.048 m` in its own footprint (parked against terrace furniture after Explore). Measured: the morning's `occupied` samples were the kerb line itself (a cross-section shows sidewalk 0.12 m above the gutter with both levels in the straddling columns), not a second floor; the keyframe z spread (0.18 m) tracks the robots riding kerbs (odometry z follows ground truth with a constant 0.149 m offset); robot_0's raster route hugged a kerb because relief up to the drop limit counted as terrain inside a cell while the footprint check applies the climb limit. | Two cycles; the kerb-hugging fix (relief between the climb and drop limits as a costed step cell) is not in these images. The Spot's furniture wedge is a local-controller and physics matter, not planning. |
| 2026-09-19 | `4ed9a34` (2D `deployment:<session>` raster of the replicated keyframes, MGG pin bump); MGG `5470174` (full-map traversability raster and A* global stage for Navigate and Return Home, unobserved cells at three times their length, in-cell relief judged by the larger terrain limit) | Benchbot Bistro, mission `0e91476b`: one cycle of the 3 m / 8 m sweeps and the long on-road test, images `mola-cleanup-4ed9a34` | The raster stage planned every explicit objective it was given (3 m: 8 to 9 m routes over 17 cells in 0 ms; returns: 35.8 m over 62 cells, 28.6 m over 50 cells, 616 and 436 expansions), and the 2D Optimised source showed the fleet composite (279 x 297 cells at 0.2 m for the four parked robots). Outcomes did not move: 3 m goals two of four (robot_2 `clearance is unavailable` at a refinement sample, robot_3's goal 1 m past the parked robot_2), 8 m goals two of four (robot_2 moved 10.6 m and stalled 3.7 m short after a `goal inside a transient disc` fallback, robot_3 6.3 m), Explore 25 to 28 m for all four, returns one of four (the Scout, 29 m in 62 s; robot_0 refused at once by `query time budget exceeded`; robot_1 7.7 m of 32 then `indexed map route is occupied or unknown` twelve times on an identical route; the Spot `start obstacle` in the raster, then the graph fallback's `known rise 0.978 m`). Measured causes: samples 12 to 23 of robot_1's first section lie on a road with two floors 0.2 m apart (surfaces -0.41 to -0.21 m in one column) and the upper floor sat inside the body box above the support layer; a goal 3 m ahead of robot_0 fell in the inflation ring of a 0.6 m object and the raster refused it while the refinement accepted it. | One cycle; whether the terrain-band collision rule, the inflation-as-cost change and the retried query budget raise the return rate is the next cycle's question. The 3 m sweep's second-row goals still land on parked neighbours by construction. |
| 2026-09-18 (later) | `a7a5f50` to `6d1e7aa`; MGG `ce97882` then `0e189ed` (scene-keyframe cap 12, height-aware retirement, disc exit rule, indexed query on its captured key, superseded-key grace 15 s, adapter tolerance for same-epoch product transitions; a one-sector 20 m scene rule was tried and returned) | Benchbot Bistro, six more cycles of the same 3 m / 8 m sweeps and long on-road test | 3 m goals arrived for all four robots in five of six cycles (the other: robot_1 `provisional terrain connector exceeds its bound` twice, before the cap of 12 and once after). 8 m goals: robot_0 six of six, Spot four of six (the two refusals were a 14 degree heading offset after its 3 m leg that pointed the goal at the sidewalk), the Scout once (kerb at the bend otherwise), robot_1 none: the same spot 10.8 m ahead was refused six times with `known rise 0.30 to 0.33 m`; replaying the sweep showed a 1.3 m strip of unknown ground exactly where robot_0 stood at robot_1's last keyframe (the ground under a masked neighbour is never observed), which a 12 s pause before the goal cured. Return legs completed 2, 2, 1, 0, 1 and 1 of 4 (best cycles `a7a5f50` and `649b27a`: 41 m and 36 m Scout and Spot returns, 48.6 m robot_0); the refusals were kerb descents of 0.17 to 0.21 m against the 0.15 m step limit, `indexed map route is occupied or unknown`, a waypoint projected onto a terrace table (z 1.35 m) and, in one cycle, two robots parked 0.9 m apart deadlocked on each other's discs (fixed in MGG `0e189ed`, not yet seen again). | The hole a departed neighbour leaves in the observer's map, kerb descents above the 0.15 m limit, and route starts beside terrace furniture remain open; the run-to-run variance is large enough that single trials do not rank small changes. |
| 2026-09-18 | `d508efd` then `5e8b627`/`91ec516`; MGG `54ca865` then `ce97882` (self-described MOLA product, product-gated authority, predecessor retained while a successor loads, concurrent per-peer builds, 15 s coherent-read age) | Benchbot Bistro, missions `6ddbf772`, `936a821f`, `cb63dc28`: 3 m and 8 m straight-ahead goals for all four robots, then the long on-road test (180 s Fleet Explore, return to each start) | Baseline (`d508efd`): the planner had no map on 76 of 122 plan requests (62%, `MOLA map snapshot is missing or stale`); the MOLA worker discarded every build whose `snapshot.json` moved on (products 10 to 27 s apart while the bridge advertised a new revision per keyframe); 3 m goals arrived for the three ground robots, no 8 m goal but robot_0's, and all four return legs failed. After the fix (`5e8b627`): 1 of 42 plan requests without a map during a 110 s explore, products every 2 to 6 s, lag 0 to 3 revisions; 3 m goals arrived for all four, 8 m for robot_0 and Spot; returns succeeded for robot_2 (27 m) and then, at `91ec516`, for robot_0, robot_1 and robot_2 (6.5, 18, 31 m) with Spot refused at once (`known rise 0.395 m` on the sidewalk). Also measured: the simulated `drift` odometry is exact (0.02 to 0.26 m after 50 m), the scan stamps are consistent with the geometry to within 0.05 m at 0.6 m/s (teleport and nearest-face probes), and the peer-body mask left no point of a passing Bunker in 526 captures. | Return legs across a kerb: the Bunker routes descend 0.17 to 0.21 m kerbs (the Bistro curbstones stand 0.16 to 0.19 m above the gutter) against a 0.15 m step limit; the Scout's 8 m goal lands on the kerb where the road bends; a parked robot's map at the start still depended on scene-change keyframes (cap raised to 12 in `a7a5f50`, not yet measured). |
| 2026-09-18 | `766fe6a`; MGG head `7be661d` (dropped waypoints, tolerated own pose, rejected proxy trimmed, proxy stepped back from measured terrain, blocked feedback for a rejected first waypoint), rebuilt Swarm-SLAM image with the yaw-aligned scene signature | Benchbot Bistro, four runs of the long on-road test: 180-second Fleet Explore, then each robot sent back along its own track to its start (7 to 45 m) with 420 s allowed | Exploration passed in all four runs (for example 44.6/38.5/32.3/23.5 m). Spot completed its return every time: 22.7 m, 33.1 m (178 s), 36.0 m (102 s) and 26.2 m (74 s). The Bunkers and the Scout did not: refused at a section whose only real waypoint (the 8 m proxy) sat on a 0.16 to 0.68 m rise (fixed twice over: trimming, then step-back, which fired 27 times in the last run), then `indexed map route is occupied or unknown` at once for robot_0, `Failed to make progress` after 7.6 m for the Scout (`route invalidated by current map: remaining route footprint intersects known terrain`, then the wedge escape found real geometry), and one adapter bug, `sleep length must be non-negative` in the paced retry, fixed in `766fe6a`. | Long goals for the Bunkers and the Scout: the graph routes cross places the map now marks as 0.16 to 0.68 m rises, and the Scout wedges physically. |
| 2026-09-18 | `e7affc8`; MGG head `3486391` (route length penalty 0.25, dropped corridor waypoints, tolerated own pose), route-progress watchdog (30 s, 0.5 m) supervising map and odometry frames, server departure sequencing, peer bodies | Benchbot Bistro, mission `4f4dfc4e` and two resets: three 180-second Fleet Explore trials from the grouped start | All three passed for all four robots: 39.6/25.6/37.4/33.0 m, 27.8/15.4/39.5/36.9 m, 23.5/38.9/33.1/36.9 m. The watchdog fired once or twice per trial (`no progress along the route for 30 s (reached 5.76 m of 9.79 m, 0.56 m off the route)`) and the existing recovery replanned; no robot ended blocked. Before the watchdog, the same stack with the 0.25 route penalty passed one of three (robot_1 or Spot at 2.5 to 3.5 m). | Long on-road Navigate goals on this stack; the rebuilt Swarm-SLAM image with the yaw-aligned scene signature. |
| 2026-09-17 | `1ddd34d`; MGG head `c0c881e` (detour guard reverted; peer bodies as transient discs; gain on leaves only, clustered within 1 m), server-side departure sequencing (`fleet.departure`), reservation guard on the component transform | Benchbot Bistro, grouped start, 180-second Fleet Explore trials in sets of three: timed 8 s stagger alone (2), server sequencing alone (3), with peer bodies (3), with the faster gain (3) | Timed stagger: one pass of two (robot_1 drove into parked Spot and pushed it 0.4 m). Server sequencing alone: 0 of 3, each losing one robot to Nav2 `Failed to make progress` within 3 m, once the head robot with every neighbour parked. Peer bodies: 0 of 3 by the harness, but 41.0/37.7/31.3, 3.8/32.7/11.3 and 30.3/2.6/17.9 m for the three Bunkers and Scout with Spot at 3.1 to 3.5 m; the head robot now drives straight out. Faster gain: plan requests fell from 4.6 s median (gain 4.0 s, about 700 vertices) to 0.85 to 1.1 s (gain 0.4 to 0.6 s); trials 25.3/2.5/40.2/31.5, 30.4/2.5/30.9/3.3 and 38.6/2.5/34.8/39.2 m. robot_1 stopped at 2.5 m every time: its first leaf lies 2.2 m away on the east sidewalk, the graph route to it loops 10 m north, and it dithers on the 7 cm ridge at y 8.2 for the rest of the trial with Nav2 never reporting a failure (it moves 0.3 m every few seconds). | A grouped start that passes the harness for all four. Open: a leaf next door reached by a long graph route (path length penalty 0.07 against 60 per unknown voxel), and Nav2's progress checker not firing on dithering. |
| 2026-09-17 | `b6c7819`; MGG head `3a548d4` (direct grid path before a detouring corridor), start poses on the crown of the road, reservations in the surveyed deployment frame, history discarded at reset | Benchbot Bistro, three consecutive 180-second Fleet Explore trials from the grouped start: missions `4d6fd111` (MGG guard only), `12e4e330`, `aa8f543a-6d28-4532-987b-02ffc4` (reservations live), with claims and exclusions traced on the wire in the last | All three passed for all four robots, after five failures in a row from the same start earlier in the day: 7.2, 23.4, 33.2, 34.7 m; then 16.3, 26.2, 28.1, 18.1 m; then 35.3, 40.5, 27.1, 19.5 m. The Scout, which had never received a path from its old start against the kerb, explored 27 to 33 m. MGG logged the guard eight times in the first trial, for example `corridor of 22.5 m to a goal 6.8 m away replaced by a direct grid path`. In the last trial all 311 claims carried the deployment frame, every planner received 172 to 248 non-empty exclusion messages, and the last targets lay north-east, north, south and south-east of the start. The reset discarded 70 earlier missions (1.19 GB) from the replica store and emptied 11 GB from the peer maps volume. One robot in the first trial ended `blocked` on Nav2 `Failed to make progress` after 23 m. | Long Navigate goals, which were not rerun on this stack. |
| 2026-09-16 | `f71512d` images (`swarmdeck-mgg:mola-cleanup-f71512d`, MGG `74e1a2d`), merges off (strict cslam image), peer-body mask ON | Benchbot Bistro, mission `bd43da59-a52a-4caa-a97b-69cc246295f5`: 3 m Navigate from the start for every robot, then a 180-second Fleet Explore | Navigate 3 m ahead from the cold start: robot_0 arrived (0.219 m remaining), robot_3 arrived (0.223 m); robot_1 rejected with `indexed map provisional terrain connector exceeds its bound` (1 keyframe), robot_2 rejected with `indexed map route is occupied or unknown` (goal beyond the Scout's first ground ring). Exploration: R0 35.9 m, R2 39.4 m, R3 35.2 m, R1 2.2 m blocked after `Failed to make progress`. Mask counters: 0.2 to 2.4 million points dropped per peer, zero stale-pose skips. This is the configuration to keep. With the mask off, every neighbour at the grouped start was baked into each robot's persistent map as occupied cells and surface samples 0 to 0.7 m high that free-space rays never remove; 10 to 30 m later the Scout still had a phantom Bunker under its footprint (`known rise 0.239 m`) and robot_1 one 0.8 m ahead (`known rise 0.373 m`), so Navigate near the start failed almost always. | Cold-start Navigate beyond the first ground ring for a robot with one keyframe. R1's chronic stall at its start position. Merges remain off (vertical bias, see the merged-floor row). |
| 2026-09-16 | `f71512d` checkout mounted; merges off (`swarmdeck-cslam:explore-color-efd0405159e9`, strict admission), peer-body mask off; four controlled 180-second Fleet Explore runs on Benchbot Bistro, drift odometry | Missions `c335ffae`, `dfb00e76`, `05757a5d`, `0fb330c2` | Maximum displacement R0 / R1 / R2 / R3 in metres. Rebuilt images (`mola-cleanup-f71512d`, MGG `74e1a2d`): 22.8 / 2.6 / 5.4 / 4.7 and 30.3 / 2.6 / 1.0 / 22.6. Old MGG `c31425e` with rebuilt sim and mapping: 23.9 / 17.3 / 1.0 / 3.0. Complete accepted image set (`c31425e` sim, MGG and mapping): 41.8 / 21.6 / 13.4 / 5.7, the only run passing the 5 m gate on all four. Stalled robots differ between runs and show 145 to 159 path samples (a replaced path about every second) and Nav2 `Failed to make progress` against neighbours; `0 vertices` lattice results and `not coherent` loader failures occur in the passing run too (25 and 113 times) and are not the discriminator. The sim images differ only in `adapters/` (Navigate and Home continuation code that exploration does not call); apt, pip and the ROS workspace are identical. | An image regression: two failures against one pass with a marginal robot is variance at the grouped start, not proof. Full coverage. Exploration with merges enabled (see the merged-floor row). |
| 2026-09-16 | `f71512d`; `swarmdeck-mgg:mola-cleanup-f71512d` (MGG pin `74e1a2d`: tilt tolerance and loader coherence retry), bridge solver-noise tolerance, `swarmdeck-cslam:mola-cleanup-f71512d`, peer-body mask on | Benchbot Bistro, mission `0e9241e0-e58f-4fa0-9306-28ba8e8dc6c4`, 180-second Fleet Explore, drift odometry, native MOLA | Coherence failures gone: 0 `not coherent` planner failures in ten minutes (104 in the previous trial), 7 `snapshot is missing or stale`. All four robots executed paths (robot_2 built a 389-vertex graph) but displacement was low: R0 1.32 m, R1 2.66 m, R2 0.60 m, R3 1.66 m; R0, R1 and R3 ended blocked after Nav2 `Failed to make progress` and a recovery timeout, R3 with `wedge escape made no progress; the robot is against real geometry`. robot_2 produced 121 keyframes and 205 verified closures while moving 0.6 m. After Stop All the authority transforms and correction revisions stayed constant for 25 s while solution orders advanced, so idle solver results no longer rebuild the map. 18 transient stale-frame rejections tolerated. | Exploration progress at the grouped start. Peers are masked out of the persistent map, so the global route ignores neighbouring robots that Nav2's live costmap still blocks on; peer current positions are not yet fed to MGG as exclusions. The robot_2 keyframe burst (rotation during recovery) is unexplained. |
| 2026-09-16 | `122fa38`; `swarmdeck-mgg:mola-cleanup-122fa38` (MGG pin `043fbd7`, tilt tolerance), `swarmdeck-cslam:mola-cleanup-122fa38` (new admission), peer-body mask on | Benchbot Bistro, mission `a92a034d-f4dc-4a25-be29-dec7c1357cdc`, 180-second Fleet Explore, drift odometry, native MOLA | All four robots executed exploration paths. Maximum displacement: R0 2.89 m, R1 9.87 m, R2 4.50 m, R3 3.17 m; R3 ended blocked after a controller progress failure and a recovery timeout. Peer closures verified/rejected: R0 93/47, R1 105/37, R2 45/9, R3 43/19; the fleet formed one verified four-robot component before the trial (pairwise inter-robot distances within 6 mm and 0.01 degrees of ground truth on the previous mission). 17 transient stale-frame rejections were tolerated. Planner reasons in the last ten minutes: 104 `MOLA index is not coherent with the mapping snapshot`, 6 `mapping manifest does not match authority key`; robot_0 completed 4 plans in 15 minutes. Corrections applied per robot: 17 to 20 in three minutes; the product was rebuilt every 10 s even while idle (revision 78). | The 5 m displacement gate (three robots failed it). Coverage. The churn and the one-second snapshot-to-index window are the next blockers; fixes are the solver-noise tolerance (`d96654e` line) and a bounded loader retry. |
| 2026-09-16 | `e610d85`; `swarmdeck-mgg:mola-cleanup-e610d85` (MGG pin `b153e63`), `swarmdeck-cslam:mola-cleanup-e610d85`, peer-body mask on | Benchbot Bistro, mission `c8f502b2-e212-4063-9e3d-55883d36cbe8`, two 180-second Fleet Explore attempts | First inter-robot merges ever recorded live: all four robots joined one verified component within seconds (robot_0 13 closures, robot_1 6, robot_2 5, robot_3 8, none rejected). Both attempts failed: the harness aborted on the component change, and afterwards every plan request rejected all lattice cells as collision (for example 4812 of 4813 free cells) although the planner grid was unchanged between revisions 4 and 5. Cause: the authority transform carried about 0.004 rad of pitch and roll once a component frame came from a closure, and MGG's MOLA backend refused any tilt above 1e-6 in its footprint and cylinder queries. Fixed in MGG `043fbd7` (0.02 rad tolerance). The mask reported 1300 to 2100 masked points per scan with zero stale-pose skips. | Any exploration result; the harness did not yet tolerate merges. The earlier reset on this stack also failed navigation bring-up under drifted nav2 1.3.13 packages (see the Jazzy apt drift row in known issues). |
| 2026-09-16 | MGG `swarmdeck` head `494ce663f14fd3d5672ec8d9ff0c5494c2be4574` (5 commits over the pinned `b153e63`), dev image `mgg-explore-dev:jazzy` | Local workstation: unified Explore and blocked-corridor feedback, native tests only, no simulation | From-scratch Release build with tests: colcon summary 340 entries (319 GoogleTest cases), 0 failures, reproduced independently after the agent's run. Only three suites changed: planning stages 11 to 19, grid refinement 37 to 43, objective service 66 to 73. No `.srv` or `.msg` changed. The legacy `PlannerSrv` Explore refinement was moved to the objective grid budget before merging. | Any live exploration result. `MGG_REV` was not advanced; a fresh Bistro exploration trial with this head is required before pinning. |
| 2026-09-16 | `cslam_lidar.yaml` at `1459c60`, `deploy/patches/cslam-registration-overlap.patch`, image `swarmdeck-cslam:planning-verified` (symlink-installed Python, no rebuild) | Offline replay of the two recorded four-robot Bistro missions (`sessions/ablation/homog`, `hetero_drift`, 16 keyframes per robot) through cslam's own ScanContext and TEASER++ with `adapters/test/ros/cslam_admission_bench.py`; reports in `sessions/cslam-admission/` | Shipped 0.95 similarity / 250 inliers: 0 candidates on both missions. New 0.70 / 18 / overlap 0.40: homogeneous 127 candidates, 115 accepted, 6 of 6 robot pairs linked, 0 false merges (over 0.5 m or 5 deg), 0 of 48 distant control pairs leaked, translation error 0.003 to 0.234 m; heterogeneous with drift 117 candidates, 105 accepted, 6 of 6 pairs, 0 false merges, 0 of 48 leaks, 0.005 to 0.476 m. Without the overlap gate the same thresholds admit 1 and 4 false merges. Two-peer ROS smoke passes with the patched code; setting the overlap gate to 1.50 yields zero closures, proving the parameter reaches the gate. | A live four-robot run: DDS, the broker budget under real timing, and the optimizer's response to over 100 new edges are unexercised. The `swarmdeck-cslam` image must be rebuilt with the new patch before any live claim. |
| 2026-09-16 | MGG `swarmdeck` branch `b153e639a1778bb747f32c29a5804fe4fc03677b`; `swarmdeck-mgg:ported` `aff2ac6de51f`, `swarmdeck-mgg-msgs:ported` `024f6e0ceece` | Local workstation: patch-series port verification, no simulation | Replaying the 59 `Dockerfile.mgg` patches in order onto upstream `902e868` produced git tree `b5bce149427069388c0c78b59840c2224054fa81`, identical to the branch commit. Test-enabled build ran 21 executables with 298 GoogleTest cases (colcon summary 319 entries, 0 failures) three times with no flakes. The image built from the migrated Dockerfile passed the native PCI contract smoke. The msgs-only build and the full image produced identical RIHS01 type hashes for all 46 interfaces (`PlanObjective` `RIHS01_bda559240a2cefd423e0d5ffe06c0f49b5d06aeca690de0f8562e839fdabd5c9`). Python: 762 deployment and adapter tests passed. | Any motion result. `Dockerfile.robot-ros2` itself was not rebuilt; its Humble msgs path was checked on `ros:humble-perception`. |
| 2026-09-16 | `c31425e`, Release ARGoS, simulation and MGG images | Benchbot Bistro, 180-second Fleet Explore, drift odometry, native MOLA | R0 40.0 m maximum displacement / 7 completed routes / 0 controller progress failures; R1 12.8 m / 4 / 2; R2 42.3 m / 8 / 0; R3 31.3 m / 5 / 0. R1 recovered from both movement failures and continued exploring. All four retained stable mission, component and frame identities, with 156 observations each and no observation errors. Stop All cleared every active route. The exact image passed all 21 native test executables (288 GoogleTests). | Full Bistro coverage or hardware use. Displacement is the maximum from the initial pose in each robot's stable navigation frame, not cumulative travel. Three local timing-sensitive failures passed on a filtered rerun. |
| 2026-09-15 | `6e7ffd2`, `swarmdeck-{argos,sim,mgg}:step15-6e7ffd2`; ARGoS, simulation and MGG image digests recorded on Benchbot | Bistro, mission `0a97e2a6-b1c9-4efe-8bf4-f88a5d8cd278`, domain 220 | Live MGG parameter queries confirmed `max_step_height` of 0.15/0.15/0.15/0.30 m for R0 to R3. All 115 focused Python tests pass; Black passes on 349 files; the native Jolt suite passes ten cases: 14/15 cm crossings, rejection at 17 cm, Spot's 29/30/32 cm boundaries, and clearance, dynamic-body and missing-support guards. The R0 20 m / -2 m lateral-offset trial failed after 82.9 s and 11.18 m reported displacement; recovery exhausted its reachable search after 2,405 expansions in 2,441 ms; the first reported footprint rejection was a 0.164 m drop against the new 0.150 m limit; maximum local and global endpoint XY errors were both 0.002 m, with no invalid endpoint samples. Stop All verified cleanup. The restart exposed a catalogue bootstrap limit: history exceeded 128 robot/mission sources and the unfiltered request returned 413, while the active-mission request still returned four ready components. All 61 replica-catalogue and live-map tests pass. | The road crossing remains unqualified. The first rejection does not identify the full barrier to every possible detour. |
| 2026-09-15 | `f97cdbf`; MGG `road-home-b511457`, UI `road-home-29afed0`, `swarmdeck-fast-livo2:road-home-f97cdbf` | Bistro sensor-based odometry comparison, mission `02e391e6-9fc5-4643-9219-8c500360814b`, domain 219 | All four Fast-LIVO2 nodes initialized gravity and converged on live LiDAR/IMU data; generated extrinsics matched Bunker, Scout Mini and Spot. Latest odometry sample ages were 0.05 to 0.18 simulated seconds; average estimator compute about 23 to 26 ms per robot; wall-clock odometry rates about 2.4 to 2.7 Hz at the observed roughly 0.3 real-time factor. Four profile tests pass, plus 29 launch and acceptance tests and Black on all 348 tracked Python files. R0's 20 m / right-offset Navigate trial failed after 73.7 wall-clock seconds and 10.98 m reported displacement; maximum local and global endpoint XY errors were 0.001 m with no invalid samples; recovery exhausted its reachable search after 1,096 expansions in 1,018 ms; the first reported rejected footprint rise was 0.123 m. R0 Home then failed immediately, before route following, on a reported 0.205 m footprint drop against the 0.100 m step limit. A planner-only shadow with a 12 m margin, 0.5 m lattice and four-second deadline exhausted its deadline after 2,883 expansions. Stop All verified cleanup. | Attributing the road failure solely to synthetic drift or to an insufficient planning deadline. Whether the Home veto represents a central ground drop or a rejected part of the current footprint. The shadow's separately accumulated map differed from the active planner's map. |
| 2026-09-15 | `b511457`; `swarmdeck-mgg:road-home-b511457`, UI `swarmdeck-ui:road-home-29afed0` | Bistro drift, mission `17e628f4-1df1-4e6c-9537-ff150a711593`, domain 218 | 228 native cases across 18 targets; 26 Python launch and acceptance tests; Black on 348 files. R0 Navigate 20 m / 2 m right: recovery failed after 10.56 m displacement, 31.3 s, 9.78 m remaining. R2 Navigate 12 m / 4 m left: arrived, 22.3 s, 0.218 m. R2 rolling graph Home: arrived, 22.7 s, 0.236 m. R3 Navigate 12 m: arrived, 42.1 s, 0.211 m. R3 rolling graph Home: timed out after 3.10 m displacement, 301.0 s, 9.24 m remaining. R0 rolling graph Home with the corrected harness: arrived, 70.8 s, 0.221 m; maximum global Home endpoint XY error 0.042 m within the 0.05 m acceptance limit; an independent simulator sample measured 0.321 m from nominal spawn; seven optimization-order changes occurred; 31.75 m maximum component-frame displacement. The passive observer measured R2 within about 0.18 m of its initial position after Home, then about 10.45 m physical displacement while parked during R3's Navigate trial. R0's final recovery rejected the bounded search after 1,622 expansions and 2,008 ms with a first reported 11.5 cm footprint rise. For the R2 12 m / 4 m left destination near world `(-10, -8)` the Scout footprint spans about 1.84 cm in height and the sampled straight approach at most 4.46 cm, while direct and smaller-offset approaches encounter the diagonal curb's roughly 17 to 19 cm discontinuity. Stop All verified the fleet idle after each trial. | R3 Home, or the R0 road crossing. An idle control state does not establish physical immobility. Component-frame displacement is not a direct physical travel measurement. The old rejected R2 endpoint remains an unresolved mapping question. |
| 2026-09-15 | `29afed0`; `swarmdeck-mgg:road-home-29afed0`, `swarmdeck-ui:merged-count-97214af` | Bistro, mission `59456af8-66aa-4eae-b5cb-4c294bfeee8a`, domain 217 | All 227 native cases across 18 test targets passed, including 37 grid-refinement and 59 objective-service cases, with no failures or disabled tests. R3 Navigate 12 m: arrived, 22.8 s, 0.245 m qualified arrival error. R3 rolling graph Home: arrived, 24.0 s, 0.162 m; the passive simulator observer measured a 0.168 m physical return error. R2 Navigate 12 m: endpoint terrain rejection, 1.0 s, no motion; it rejected a known 0.397 m footprint rise at its endpoint, and rechecking `(11.99, 0.15, -0.17)` at map revision 3565 still rejected a 0.394 m rise. R3's last observed local and global endpoints had zero XY error relative to the requested component goal in both trials. The R0 20 m / right-offset probe exhausted its two-second planning deadline after 4455 projections and 1939 expansions; the 30 m / left-offset R0 probe and the 12 m R2/R3 probes passed, including exact XY and live lookahead validation. API and native-smoke goal transforms agreed within 4.2 mm vertically and exactly in XY and yaw at a shared snapshot. The 38-case replica UI suite, Svelte checks and the production build passed. Stop All verified the fleet idle after each trial. | Fleet-wide navigation. Neither the R0 road crossing nor the commanded R2 destination is qualified. It does not establish a collision mesh defect at the rejected endpoint. |
| 2026-09-15 | Planner-only shadow containers on the same live R0 mapping topics | Bistro Navigate and Home search tuning, no motion commanded | 0.25 m lattice / 2 s deadline: deadline exhausted. 0.25 m / 4 s: deadline exhausted. 0.5 m / 2 s: one route accepted, one deadline exhausted. 0.5 m / 4 s: three routes accepted and live lookahead validation passed. Fixed-goal shadows at both 0.25 m and 0.5 m resolution later exhausted their four-second deadlines. | A controlled same-goal benchmark: the maps and relative destinations were changing, and the shadows integrated separate live cloud histories. A fresh motion trial remains the acceptance gate. |
| 2026-09-15 | `a17d2a9`; `swarmdeck-mgg:nav-window-a17d2a9` | Isolated native container, then a fresh Bistro mission on domain 216 | All 15 core and three ROS test targets passed: 219 cases, including 35 grid-refinement and 53 objective-service cases; the authoritative XML and build log carry prefix `test-parent-native-20260915T044103Z`. R0's 30 m left-offset route and the 12 m R2/R3 routes passed native planning and live validation. R0's 20 m right-offset goal exhausted three two-second planning attempts before meaningful motion; the final attempt made 6,464 projections. R3 reached its 12 m Navigate goal in 23.4 s with a qualified arrival error of 0.219 m and zero local and global endpoint error. R3's subsequent Home trial used rolling graph continuation but failed about 3.55 m from home: an occupied intermediate corridor pose was rejected before any traversal or grid expansion. Stop All verified the fleet idle after both trials. Mesh investigation distinguished an abrupt 16 to 17 cm direct curb from an eastern passage with successive 6.8 cm and 7.5 cm rises. | The R0 20 m route. R3 Home. The eastern passage is geometry evidence, not a successful native or physical crossing. |
| 2026-09-15 | Isolated Benchbot container with a four-core CPU allowance | Native MOLA replay of 147 Bistro keyframes (602,112 endpoints) | The metric map and planner grid published in 1.97 seconds, including process startup and serialization. All endpoints and surface samples were retained, 3,999,967 ray steps were used, and peak native RSS reached 180.3 MiB. | A worst-case bound. The one-million-point publication limit is still hard: a map whose captures each retain the configured maximum of 4,096 endpoints reaches it at 245 captures. |
| 2026-09-14 | `8384392`; `swarmdeck-mgg:nav-window-8384392`, `swarmdeck-ui:merged-count-97214af` | Fresh four-robot Benchbot Bistro mission | Exact native routes passed for R0 at 20 m forward / 2 m right and 30 m forward / 2 m left, and for R2 and R3 at 12 m forward; planning took 966, 270, 545 and 210 ms respectively, and every returned route retained its requested XY and passed live validation. These probes did not command motion. The 20 m R0 motion trial travelled about 10.8 m reported displacement before recovery exhausted three attempts near the raised road section; the final search used 1394 ms and reported no detour within its window, with a known 11.5 cm rise exceeding the Bunker's 10 cm limit. A static check using the Bunker's extended 1.073 by 0.828 m planning footprint left known-pavement connectivity disconnected at 8, 12 and 24 m margins. The complete native objective suite passed all 52 cases with the final patch sequence. At UI verification the legacy map service reported zero merged members while the active replica catalogue contained a verified three-robot component and one separate robot; all 33 named replica UI tests, Svelte checks and the production build passed. | A failed arrival trial. The static check does not rule out a side passage outside the sampled pavement or through currently unknown terrain, and it does not justify raising the step limit. One rolling Home trial was inconclusive because the harness stopped before the final segment. |
| 2026-09-14 | `42a5129`, `3e73f75`, `233911a`, `e2361a4` | Benchbot four-robot GPU Bistro with full camera resolution | A read-only R0 request 20 m forward and 2 m right exhausted the 500 ms budget; with a 2000 ms explicit-objective budget the same relative request returned all 104 route poses in 857.5 ms and passed live validation in 15.7 ms. A 30 m R0 route passed with the original 500 ms budget, and an R1 12 m route passed with both profiles. Local failure evidence contained only 46 to 106 expansions despite hundreds of terrain queries. Catalogue profiling found 104 robot publications across 100 components: reading their JSON took 212 ms, normalizing them 3811 ms, and assembling the index 307 ms, while the active mission alone took 11, 16 and 3 ms; version lookup stayed below 1 ms in both cases. Combined adapter, live-mapping and launch regressions passed (125 tests); all 58 replica, component, live-view and deployment regressions passed. | An arrival trial: the first motion harness aborted on a live-view HTTP timeout and the repeat failed during observation setup. Increasing a deadline does not resolve an occupied endpoint or authorize stale map data. |
| 2026-09-14 | `39d9a72`, `cffc9ac`, `a806157`, `93abb6b` | Bistro R0 road detour and physical Home trial | R0 followed a 12 m relative Navigate request for approximately 10.9 m reported displacement; new observations rejected its endpoint for a 53 cm height difference and exhausted bounded recovery. From that location Return Home succeeded in 139.8 s, with a retained graph of up to 18 poses and execution progressing through `following_local` and `following_final`; independent simulator truth measured 14.458 m from spawn before the action and 0.248 m at completion. Stop All then verified all four robots idle. Static Bistro pavement checks found a diagonal curb approximately 17 cm high across R0's original four-metre detour window. After the catalogue cache update the unfiltered live catalogue took 615 ms in a bounded repeat, compared with roughly five seconds before; the active-mission live view took seven milliseconds. The combined navigation, Home telemetry, replica and launch suite passed all 188 tests, then 193 after a provenance check; Black accepted all 348 tracked Python files. | Successful arrival for the Navigate leg. Controller efficiency: the final Home approach included substantial manoeuvring. The measurements are individual observations, not latency percentiles or a cold-start guarantee. |
| 2026-09-14 | `ec84488` plus the authority-heartbeat and Home-handoff corrections | Reduced single-robot local Bistro trial, then a fresh-mission repeat | Native MGG: six packages built, 21 CTest targets containing 242 GTest cases passed. Server, map, replica, navigation and adapter regressions: 255 passed. 14 UI test files passed; Svelte and the production bundle passed; five native MOLA CTests and the persistent JSONL smoke passed; final objective, authority and peer-launch regressions passed 92. The initial four-robot run used software Vulkan and measured a real-time factor of 0.036, with no authority heartbeat during a 15-second probe. The reduced one-robot trial measured 0.368 real-time factor; all 13 authority messages received during a 15-second probe passed validation; a native three-metre route request took 20.1 ms and its route validation 12.2 ms. R0 reached the first 12 m destination and reported success after 128.6 wall seconds with 11.905 m reported displacement; a post-arrival component query returned HTTP 404; Return Home followed the first approximately eight-metre graph section then failed the final-chunk endpoint check by 0.018 m. All 77 objective planning tests passed, including the observed 18 mm correction case. The repeat with both fixes passed: Navigate success after 86.3 wall-clock seconds with 11.988 m reported displacement; Return Home success after 100.3 s with 11.702 m reported displacement. Stop All was verified after both actions. | An independent simulation-ground-truth error bound: these are action and odometry observations. Four-robot Bistro motion, inter-robot closures, dynamic obstacles and full-resolution rendering. The reduced 160×120 camera changes depth sampling, so this trial cannot qualify thin depth-only obstacles or production camera fidelity. |
| 2026-09-14 | `0e1704e`, then `1806cab` | Benchbot Bistro fleet exploration over the `planning-refactor` Git deployment | The initial Git rollout `0e1704e` produced colored chunks for all four robots and retained one server and simulation stack with the UI on port 15173. `1806cab` passes 73 exploration and peer-coordination tests, including newer-path delivery during the authority wait. In a fresh two-minute exploration trial all four robots completed routes and remained actively exploring; maximum observed pose displacement from their starts was 21.6 to 36.5 m; Stop All ended the trial; R3 then completed Return home after travelling approximately 32 m back. In the subsequent fresh three-minute bench trial R0, R2 and R3 completed exploration routes and reached maximum displacements of 33.48 m, 25.58 m and 39.74 m; R1 remained within 1.85 m of its start without completing a route, its first controller attempt reporting no progress. All four robots stopped successfully. | A passed fleet acceptance result: the cause of R1's remaining stall is not established. Displacement is not travelled path length or coverage. Colored-chunk availability verifies RGB data, not camera calibration. |
| 2026-09-13 | `f03e68f6ee1c` reviewed bundle, six images under tag `fleet-regression-f03e68f6ee1c` | Benchbot `planning-next` rebuild and dashboard reset endpoint | Native MGG and MOLA checks, the colored native map import, the peer ROS smoke and 11 server smoke tests passed. The UI served on 15173 matched the reviewed build and all four robots reported online and navigation-ready. A real reset request completed with a new mission and shared DDS domain; all four robots returned idle and navigation-ready, and repeating the completed request's ID returned its original result without a second reset. The trial caught a missing `COMPOSE_PROFILES=argos` in the host supervisor service, corrected before the passing run. Local validation: 270 focused Python tests; fleet, replica and 3D UI suites with zero Svelte errors or warnings; all four native MOLA mapping test programs; the ROS cSLAM diagnostic smoke; native MGG service fixtures returning full 25/50/100 m goals in about 160 ms; Bunker and Spot exploration fixtures returning usable paths in about 60 ms; and a clean build of the final MGG patch sequence passing 144 core and 49 ROS tests, including 37 objective-service cases, with zero failures. | Live navigation arrivals, Home, exploration coverage, inter-robot closures in the new mission, rendered camera quality or trained Gaussian reconstruction. The native fixture measurements are small synthetic scenes and do not establish Bistro latency. |
| 2026-09-13 | Live trials through the footprint, Home-budget, footprint-cell and progress-checker corrections | Bistro fleet on Benchbot port 15173 | The first rollout reached a five-metre R3 goal within 17.5 cm and produced multiple successful exploration routes on R0 and R3. Static collision geometry placed R2's eight-metre endpoint across an approximately 18 cm curb, above the Scout's 10 cm step limit; a later five-metre endpoint also showed a 17 to 18 cm discontinuity across the footprint. After the Home budget correction R2 reached the statically qualified 12 m road goal within 19.7 cm, and R3 reached a five-metre goal within 22 cm and completed Home. R2 then rejected Home before motion with a 14.3 cm known-rise diagnostic: the Scout's extended body has a 45.69 cm circumscribed radius, the query expanded ray centres to 56.30 cm and the containing voxels to 69.43 cm, and full-material static evaluation of the selected cells spanned 14.54 cm while a 56.3 cm circle centred at Home spanned only 5.28 cm. After the footprint-cell fix R2 completed the 12 m road trip and Home, with 11.86 m outbound and 11.60 m return displacement. R3 then reached its five-metre outbound goal but stayed active for 110 seconds approaching Home. After the translation watchdog change R2 completed the 12 m goal within 17.5 cm and R3 its five-metre goal within 21 cm, and both completed Home. Every peer's colored capture counter increased, all four replicas contained RGBA chunks, and the production UI bundle supported that encoding. R2 later stopped near world `(-14.47, 3.92)` where the 56.3 cm padded query overlapped a real pavement rise with a 21.14 cm full-material static span. Simulation adopted Nav2's `SimpleProgressChecker` with 20 cm translation and a 20-second simulation-time allowance; the 25 cm arrival tolerance is unchanged. | Arbitrary passage through the congested starting area. Colored-chunk availability is not visual calibration accuracy. The September 13 live trial failed the autonomy acceptance gate: R2 selected lattice routes that became empty during the terrain check after smoothing, R1 lost connectivity at the physical start, R3 rejected Home when neither endpoint had a measured floor hit, and explicit navigation exhausted the 500 ms grid deadline after thousands of terrain queries. All 88 inspected R0 keyframes were geometry-only before the timestamp fix. |
| 2026-09-13 | `swarmdeck-mgg:complete-route-review`, simulation `swarmdeck-sim:clearance-review` | Bistro complete-route acceptance, mission `3635493c-e251-46cf-aa45-e3a1c3abe9f0` | All six native packages built and 230 core, map, ROS and PCI tests passed, including a 30 m exact service response, a long obstacle detour, an unknown gap, platform step limits and existing snapshot and ownership cases. The combined local adapter, server, coordination and deployment run passed 161 tests, and a final telemetry adjustment passed all 28 affected server tests. All 20 Nav2 lifecycle nodes reported active. After a 90-second Explore warm-up, R0 was sent a single component-frame goal at a previously traversed road point 12.168 m away: the displayed route contained 50 poses and reached the selected destination, telemetry reported only `following_final` before terminal success, and R0 finished 0.159 m from the component-frame goal. Controller logs restricted to R0 contain exactly one goal received, one goal reached, zero cancellations and zero partial-segment continuations. Controller execution took 22.21 s; the API observer recorded success at 26.45 s including observation overhead. Stop All ended the trial. | Forward navigation into visible road, or that every terrain, unknown-space or Home failure is resolved. A subsequent stationary R0 probe reproduced immediate rejection: 1 m and 3 m succeeded, 6 m failed body-known clearance and 12 m failed ground support; raising the goal seed by 0.3 m changed neither failure; R2 independently passed 1 m and 3 m, then rejected 6 m and 12 m. |
| 2026-09-13 | Depth-relay, planning-frame and terrain-precision corrections in the same series | Bistro forward-goal and physical-stall investigation | The simulation camera publishes valid finite depth up to its 40 m far plane, but the relay discarded all depths at or beyond 20 m; one live R0 frame lost 545 of 4,800 sampled rays this way. With the same R2 pose and camera observations, 6 m and 12 m road goals changed from support and clearance failures to complete 21- and 41-pose paths in roughly 1 to 2 ms. A controller trial then showed local SLAM's map-to-odometry correction cancelling a valid route after 3.6 m; a repeated 12 m trial arrived within 0.146 m in 20 s but still replanned as the map gauge moved, with 1.11 m map-to-odometry translation and no graph correction (`solution_order=[0,-1]`, `correction_revision=0`). In one R2 cold-start request 339 projected positions were supported while all 338 attempted connections failed at the same unknown near-start body volume. A full 5 cm occupancy map caused ten-second request timeouts and exceeded 10 GB across the four planners, and was removed. During a fresh R2 trial both Nav2 and the ARGoS driver commanded 0.6 m/s for roughly ten seconds while the robot remained stationary, and the action failed its progress check; R1 travelled about 10 m of a 12 m route before a similar stop. | Arrival or obstacle avoidance. Sensor-faithful tests and ordinary forward controller trials are required before claiming general navigation acceptance. The relay change does not resolve the raised planning body envelope or sparse long-range ground support. |
| 2026-09-13 | `swarmdeck-sim:clearance-review`, `swarmdeck-mgg:calibration-review`, native diagnostics image `swarmdeck-mgg:diagnostics-review` | Bistro waypoint execution trial, mission `08e34c3d-c4e7-4d70-bafd-7b0f2c4a0539` | All 20 Nav2 lifecycle nodes were confirmed active with `GetState` after staggered startup. A 90-second Explore warm-up produced successful arrivals 0.202 to 0.234 m from their endpoints. Three active Explore cancellations were explicitly caused by material-correction invalidation of peer reservations rather than unsolicited path replacement; R1, R2 and R0 encountered controller progress failures and R2 exhausted recovery. R0 then received a Navigate goal 7.779 m away on its just-traversed road, completed one partial segment and then the final segment in 25.95 s, finishing 0.213 m from the component-frame goal, with two successful segment completions and zero client-requested cancellations. Between the recorded before and after authorities, translation changed by 0.105 m, rotation was unchanged, and solution order advanced from `[36, 0]` to `[38, 0]`. The combined local Python regression passed 281 tests. Stop All was sent after each trial. | Home connectivity, all obstacle detours, step avoidance or fleet exploration completion. The controller progress failures remain unresolved. A proposed physical-body change was rejected during review because exempting a whole floor voxel can hide a curb sharing that voxel. |
| 2026-09-13 | Baseline before the execution-monitoring change | Bistro, mission `3e8581c2-bd5c-4f6a-85be-6273f7f53ec8` on the same port 15173 deployment | During an operator Navigate trial, R2's controller logged 16 client-requested cancellations before its final arrival, with no intermediate successful segment completions. The captured interval also contains one R1 cancellation. | That MGG limits each path to one metre. This is premature cancellation; code inspection identified the active objective's 2 cm map-correction threshold as a source. |
| Not dated in source, between the 2026-09-11 stop and the 2026-09-13 sections | `swarmdeck-mgg:mola-graph-review` | Bistro MOLA graph-provider follow-up, mission `e5e13bd9-4097-4161-9151-4c19aa25c569` | Native core and ROS suites passed 216 tests with no failures or skips; the actual MOLA worker and MGG probe passed initial, reused-artifact and corrected-publication checks through `tests/deployment/mola_mgg_acceptance.sh`, with small fixture loads taking 7 to 11 ms. Read-only graph requests loaded maps for all four robots: R0 no vertices with 871 candidate edges reported occupied, 19 ms; R1 no vertices, 908 reported occupied and 32 steep edges, 25 ms; R2 no vertices, 321 occupied and 140 steep, 17 ms; R3 780 vertices, 7,953 edges, a 26-pose selected path, 2,056 ms, of which 1,758 ms computing gain. R0's unchanged solver results advanced its graph revision roughly every 10 seconds, and the snapshot preceded its matching MOLA index by 1.2 to 1.6 seconds. A 120-second Explore trial moved all four robots with sampled net displacement of approximately 28.55, 2.88, 18.45 and 31.35 m; R1 became blocked; warm Navigate moved R0 about 1.4 m before exhausting three recovery attempts, and the far goal and Home still failed on graph connectivity. Browser checks verified Local geometry growing from 4,096 to 60,000 points, a 1.20 m ceiling surviving live updates and view switches, and camera HLS playback reaching 320×240. Stop All left every robot stopped. | Four-robot navigation acceptance. The collision diagnostic combines unknown clearance, occupied volume and some support failures, so these labels do not establish actual obstacles. Exploration motion is not evidence of correct completion or obstacle detours. |
| 2026-09-10 (2026-09-11 UTC) | `swarmdeck-mgg:partial-route-review`, `swarmdeck-sim:navigation-map-review`, `swarmdeck-mapping:navigation-map-review` | Bistro, mission `6f6afc5c-a52a-4caa-a97b-69cc246295f5`, domain 201, four connected robots, drift odometry, native MOLA mapping | Navigation acceptance did not pass; MGG planned directly from the native MOLA product and goals were rejected before movement. |
| Not dated in source, same port 15173 era | `swarmdeck-mgg:exploration-lifecycle-review`, built on `swarmdeck-mgg:diagnostics-10`; | Bistro 120-second Fleet Explore, mission `7fb45d65-7594-4f59-b85d-ddd476639e70`, ROS domain 201, native MOLA mapping, patched PCI | All four robots accepted exploration. R0 and R2 ended about 25 m from their initial positions; R3 moved about 19 m before controller failures exhausted its recovery window after two failed attempts; R1 completed one short path then remained waiting because its local grid found no reachable continuation. The ROS trace recorded 15 nonempty paths with 8 to 36 ordered poses, and R0 and R2 completed seven controller goals between them. Three reservation cancellations remained for R3 and two for R0. An idle authority trace showed heartbeat gaps below 1.32 seconds for all robots. Stop All stopped all four robots. The focused adapter, ROS 2, coordination, objective, detector and launch-config suite passed 205 tests, and the exact benchbot MGG image passed 8 native tests. | Full coverage: R1's stall remains a planner and terrain limitation, and the run does not establish the exact cause of the remaining reservation cancellations. |
| 2026-09-10 | Based on `297508f`; `swarmdeck-mapping:parallel-review`, MGG runtime `swarmdeck-mgg:diagnostics-10` | Benchbot RTX 3080, ARGoS Bistro in the isolated `planning-next` project, mission `c1af7f0a-8014-4558-b4c2-33ebfe86418a`, domain 201, native MOLA mapping | Combined autonomy, exploration, objective, onboard-map, simulator and replica Python regression: 372 passed. Native mapping and framework checks passed. |
| 2026-09-10 | `swarmdeck-mapping:mola-runtime-review` and `swarmdeck-mapping:mola-planner-review` | Workstation MOLA runtime and planner-product measurements on `benchbot.yannbouteiller.com`, MOLA 2.9.0 Release build | 20 pose corrections of a deterministic 100,000-point cloud in one process gave median correction latency 30.0 ms and maximum 35.6 ms, compared with 163.8 ms for five full one-shot imports; native RSS ranged from a first sample of 71.1 MiB to a final 65.0 MiB, with 71.1 MiB maximum sampled. A read-only copy of the stopped Bistro fleet's four peer maps imported 991,344 points in 0.83 seconds with four coherent artifact indexes; individual maps held 214,959, 28,946, 71,213 and 676,226 points. A fresh replay requesting both native products completed in 2.38 seconds including first provider loads, five warm refreshes per component and query batches, with a maximum sampled native RSS of 174.2 MiB; all four unqualified maps retained zero free voxels. Per robot, worker publication / first provider load / median warm refresh / 8-sample query: `robot_0` 295 / 233 / 8.4 / 1.1 ms; `robot_1` 35 / 58 / 1.6 / 0.8 ms; `robot_2` 88 / 97 / 3.2 / 1.1 ms; `robot_3` 810 / 562 / 23.7 / 0.9 ms. The captured-point acceptance fixture passed in 0.24 seconds across four publications and the local autonomy suite passed 121 tests. Per robot (points / worker publication / first provider load / median warm refresh / query of 8 samples): `robot_0` 214,959 / 295 ms / 233 ms / 8.4 ms / 1.1 ms; `robot_1` 28,946 / 35 ms / 58 ms / 1.6 ms / 0.8 ms; `robot_2` 71,213 / 88 ms / 97 ms / 3.2 ms / 1.1 ms; `robot_3` 676,226 / 810 ms / 562 ms / 23.7 ms / 0.9 ms. | Worst-case latency or long-duration memory. The replay uses stopped maps with unqualified rays, so it does not measure heavy free-space carving or motion safety. It does not resolve R1's limited exploration or qualify moving-robot terrain planning. |
| 2026-09-10 | `211f8d6` | Bistro, mission `92028a23-d1d1-45b8-8d0b-509815e35950`, domain 189; passive recorder captured 562.522 seconds without dropped truth samples | R3 (Spot) passed the offline physical-arrival check with `--allow-authority-replanning`: it started 9.427 m from home, executed a 39-point route and settled within 0.216 m for 17 consecutive samples spanning 1.595 seconds. R0: no route, current pose `(7.91, 3.36, 0.25)` exceeded the 1 m graph-binding tolerance. R1: no route, home `(-0.01, 0.01, 0.07)` had no mapped ground support; it explored only 0.789 m and recorded three map revisions before local exhaustion. R2: Home was not issued because fresh authority could not be captured; its 2D SLAM node was inactive after a startup transition timeout, MGG lacked `map_frame` and odometry, and Nav2 bringup failed. Native validation passed 176 GoogleTest cases across 20 binaries including four PCI cases; colcon reported 196 results with no errors, failures or skips. A five-second read-only probe found R1 received 18 valid 320×240 depth images, eight depth-derived mapping clouds containing 38,400 finite points, and five LiDAR clouds containing 152,875 finite points, with a base-to-camera transform matching R0's. | Terrain coverage, persistent graph connectivity or reliable fleet startup. Activating R2's SLAM node afterwards was a manual recovery, not a passed fresh-start gate. Sensor delivery does not establish that MGG's terrain rejection is correct. |
| 2026-09-10 | `74c3484` | Bistro, mission `7bdcbb62-ddc3-438f-8bc5-0885fd374b1e`, domain 188; passive recorder ran 1118 seconds including setup before physics | All four robots executed paths during 30 seconds of Explore and Stop All cleared every active path. Return Home: `robot_0` executed a 37-point route and was cancelled on an authority change, final independent XY distance from start 8.380 m; `robot_1` corridor has an unsupported endpoint, no route, 8.650 m; `robot_2` current pose or goal outside the graph, no route, 9.076 m; `robot_3` executed a 38-point route and was cancelled on an authority change, 7.512 m. The offline evidence checker classified all four Home trials as failed. Cumulative navigation-transform shifts reached 21.458 mm (`robot_0`) and 25.423 mm (`robot_3`), with cancellation following within 84 ms; mission, component and optimizer correction revision stayed unchanged. Native Jazzy validation passed 169 GoogleTest cases across 19 binaries, including 17 objective-service cases; a complete bent-path fixture took 78 ms including map construction. | Arrival. The existing 20 mm validity guard correctly rejected the routes, and routine map revision changes alone did not cancel them; authority-change recovery and terrain coverage still require investigation. |
| 2026-09-10 | `bff4fb4` | Bistro on the workstation, mission `126bed69-9327-4d8f-a4b2-075fa7569d8e`, domain 187, newly built MGG and mapping images; more than 2,100 truth samples per robot without dropped samples | All four robots executed paths during a 30-second Explore observation, all four replica revisions advanced, and Stop All cleared every active path. Scout reported blocked, one Bunker reported local exhaustion after less than a metre, and Spot lost a reservation. Every Return Home request failed before producing a route: `robot_0` current pose or goal outside the graph, 9.438 m from start; `robot_1` current pose or goal has no mapped terrain support, 0.773 m; `robot_2` corridor has an unsupported endpoint, 6.888 m; `robot_3` outside the graph, 6.861 m. Persistent graph growth stalled at six vertices for `robot_0` and one for `robot_3` despite local exploration movement. | Coordinated coverage or closure gates: the peers remained in four separate components. Native fixture success does not close the Bistro acceptance gate. |
| Not dated in source, before `bff4fb4` | Staged refactor clean export | Isolated four-robot Bistro ARGoS runs on the workstation, missions `88492d31-5c28-4de9-bbbd-8bfb1a74014d` and `b7f9edbb-4e97-4dcb-93bd-841baf074dd0` | The first bounded exploration run produced keyframes and durable replicas from all four robots (11, 14, 10 and 44 keyframes). With only the test server stopped for 20 seconds, scan normalization continued on every robot and robot 0 advanced its optimized map from revision 81 to 83 while its acknowledged revision stayed at 81; after restart, replication advanced to revision 84 and the error cleared. In mission `88492d31` all four robots executed paths during a 90-second Explore trial, with independently recorded ground-truth displacements of approximately 12.7, 3.8, 15.1 and 25.2 m and 26, 16, 32 and 54 captured keyframes with no dropped keyframe pairs; robot 1 encountered repeated controller progress failures, and all four reported stopped, inactive navigation and empty paths after Stop All. An earlier Return Home trial produced no route and no arrival, with independent ground truth staying approximately 26.3 m from home. In mission `b7f9edbb` a 25-second Explore trial with all four robots passed authority validation on Return Home, but Spot's first global vertex was inserted after it had moved about 1.4 m, Home produced no route, and independent ground truth remained 6.32 m from home. The local two-peer Swarm-SLAM fixture verified a known 0.4 m translation and 6-degree yaw without a server, and the corrected image passed the joint-solution fixture in about 25 seconds. | Exploration coverage or successful fleet coordination: the maps remained in four disconnected components, no inter-robot alignment was validated, and singleton optimizer results were accepted without pose corrections. Arm64 builds, physical robot trials, transparent peer restart and comparative four-robot coverage are separate gates. |
| 2026-09-09 | Baseline before grid refinement, clean export of the staged refactor | Local and authorized amd64 workstation contract validation | The Python autonomy, adapter, replication, reconstruction, command-routing and backend batch passed 332 tests; Svelte type checks and the production build passed; the browser rendered a local 7,800-point replica fixture. ROS Jazzy Swarm-SLAM compiled with the versioned message patch and its native Open3D, TEASER and message imports passed; MOLA 2.9.0 compiled and passed native correction and replacement tests plus the Python-to-MOLA serialization smoke. A later affected Python suite passed 291 tests from a clean commit export, and the Gaussian frame-ID checks passed in the 45-test reconstruction suite. In an isolated headless browser a six-second network interruption retained the displayed map and its 1.15 m ceiling. The deterministic VLP-16 indexed-query test uses nine captures over four travelled metres: accumulated ground rays certify a Bunker-sized 1.6 m forward corridor, an unseen interval stays `UNKNOWN`, and a wall is `OCCUPIED`. A local 15-sample flat-surface microbenchmark measured median and p95 terrain query times of 0.481 and 0.512 ms after the change, versus 0.497 and 0.531 ms before. The native CUDA Gaussian smoke's three-view fixture completed nine optimizer iterations and converted 540 Gaussians. | Physical map accuracy or GPU performance on deployed robots. The microbenchmark is not a Jetson latency or terrain-accuracy qualification, and the Gaussian fixture does not establish reconstruction quality. |

Commits after `c31425e` (`440122b`, `2f2871b`, `6ab2b4d` and `a2dc891`) have no
live trial yet.
