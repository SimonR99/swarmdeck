--- a/cslam/src/back_end/decentralized_pgo.cpp
+++ b/cslam/src/back_end/decentralized_pgo.cpp
@@ -944,6 +944,7 @@
     gtsam::GncOptimizer<gtsam::GncParams<gtsam::LevenbergMarquardtParams>>
         optimizer(*graph, *initial, params);
     result = optimizer.optimize();
+    optimization_succeeded_ = true;
   }
   catch (const std::exception &e)
   {
@@ -958,6 +959,7 @@
     catch (const std::exception &e)
     {
       RCLCPP_ERROR(node_->get_logger(), "Logging failed: %s", e.what());
+      optimization_succeeded_ = false;
       result = *initial;
     }
   }
@@ -1025,7 +1027,17 @@
     logger_->log_initial_global_pose_graph(aggregate_pose_graph_.first, aggregate_pose_graph_.second);
   }
 
+  // Collection still runs every round so remote-only changes are observed.
+  // Include priors, initial values and epochs; graph size alone is insufficient.
+  if (unchanged_graph_.matches(aggregate_pose_graph_.first,
+                               aggregate_pose_graph_.second,
+                               optimization_epochs_, origin_robot_id_)) {
+    optimizer_state_ = OptimizerState::IDLE;
+    return;
+  }
+
   // Optimize graph
+  optimization_succeeded_ = false;
   optimization_result_ =
       std::async(std::launch::async, &DecentralizedPGO::optimize, this, aggregate_pose_graph_.first,
                  aggregate_pose_graph_.second);
@@ -1045,6 +1057,11 @@
       return;
     }
     optimization_count_++;
+    if (optimization_succeeded_) {
+      unchanged_graph_.remember(aggregate_pose_graph_.first,
+                                aggregate_pose_graph_.second,
+                                optimization_epochs_, origin_robot_id_);
+    }
 
     // Share results
     share_optimized_estimates(result);
--- a/cslam/include/cslam/back_end/decentralized_pgo.h
+++ b/cslam/include/cslam/back_end/decentralized_pgo.h
@@ -35,6 +35,7 @@
 #include <thread>
 
 #include "cslam/back_end/gtsam_utils.h"
+#include "cslam/back_end/unchanged_graph.h"
 #include "cslam/back_end/utils/logger.h"
 #include "cslam/back_end/utils/simulated_rendezvous.h"
 
@@ -384,6 +385,8 @@
         std::future<gtsam::Values> optimization_result_;
 
         gtsam::GraphAndValues aggregate_pose_graph_;
+        UnchangedGraph unchanged_graph_;
+        bool optimization_succeeded_ = false;
 
         rclcpp::Publisher<cslam_common_interfaces::msg::OptimizerState>::SharedPtr
             optimizer_state_publisher_;
--- /dev/null
+++ b/cslam/include/cslam/back_end/unchanged_graph.h
@@ -0,0 +1,36 @@
+#pragma once
+
+#include <gtsam/nonlinear/NonlinearFactorGraph.h>
+#include <gtsam/nonlinear/Values.h>
+#include <cstdint>
+#include <vector>
+
+namespace cslam {
+// Compare the collected graph, not just local counters: a stationary optimizer
+// must still solve when a remote robot adds a keyframe or loop closure.
+class UnchangedGraph {
+ public:
+  bool matches(const gtsam::NonlinearFactorGraph::shared_ptr& graph,
+               const gtsam::Values::shared_ptr& values,
+               const std::vector<uint64_t>& epochs, unsigned int origin) const {
+    return graph_ && epochs == epochs_ && origin == origin_ &&
+           graph_->equals(*graph, 0.0) && values_->equals(*values, 0.0);
+  }
+
+  void remember(const gtsam::NonlinearFactorGraph::shared_ptr& graph,
+                const gtsam::Values::shared_ptr& values,
+                const std::vector<uint64_t>& epochs, unsigned int origin) {
+    // Inputs are immutable once dispatched to the async optimizer.
+    graph_ = graph;
+    values_ = values;
+    epochs_ = epochs;
+    origin_ = origin;
+  }
+
+ private:
+  gtsam::NonlinearFactorGraph::shared_ptr graph_;
+  gtsam::Values::shared_ptr values_;
+  std::vector<uint64_t> epochs_;
+  unsigned int origin_ = 0;
+};
+}  // namespace cslam
