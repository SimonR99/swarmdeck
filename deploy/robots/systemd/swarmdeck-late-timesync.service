[Unit]
Description=SwarmDeck: report (or repair) a clock correction that landed late
After=docker.service

[Service]
Type=oneshot
EnvironmentFile=-/etc/default/swarmdeck-timesync
ExecStart=/usr/local/bin/swarmdeck-late-timesync
